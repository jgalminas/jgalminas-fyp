import { render } from "@testing-library/react";
import { TimeCursor } from "./Editor";

describe("x", () => {

  it("", () => {
    render(<TimeCursor position={0} setPosition={() => {}} offset={0} maxWidth={100} />);
  })

})